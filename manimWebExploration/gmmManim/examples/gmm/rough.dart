// Idea: Put point on top of each gaussian where user can move them around
// Idea: Put Another numberline under graph with the dots colored with their soft alignments

// Future animateData1D(List<Dot> dots, List<Dot> dots2) async {
//   // await playMany([
//   //   for (var i = 0; i < dots.length; i++) Transform(dots[i], target: dots2[i])
//   // ]);
//   await play(Transform(dots, target: dots2))
// }
