Put point on top of each gaussian where user can move them around
Put Another numberline under graph with the dots colored with their soft alignments
Visualize the soft alignment of each gaussian each point is